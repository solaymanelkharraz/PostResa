import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { 
  Home, 
  Calendar, 
  MessageSquare, 
  Bell, 
  Search, 
  MoreHorizontal, 
  Heart, 
  MessageCircle, 
  Share2, 
  MapPin, 
  Clock, 
  Plus,
  User,
  CheckCircle,
  Video
} from 'lucide-react';

// --- Mock Data: Feed ---
const POSTS = [
  {
    id: 1,
    author: "M. El Kharraz",
    role: "Prof. Développement Digital",
    avatarColor: "bg-indigo-500",
    time: "2 hours ago",
    content: "📢 Reminder: The submission deadline for the React Project (Atelier 4) has been extended to this Sunday at midnight. Please ensure your GitLab repositories are public.",
    image: null,
    likes: 24,
    comments: 5,
    tag: "Announcement"
  },
  {
    id: 2,
    author: "Administration",
    role: "Direction Pédagogique",
    avatarColor: "bg-rose-500",
    time: "4 hours ago",
    content: "🚀 Hackathon 2025 is coming! Join us for a 24-hour coding marathon next weekend. Teams of 4. Registration is now open via the reservation tab.",
    image: "https://images.unsplash.com/photo-1504384308090-c54be38558bd?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    likes: 156,
    comments: 42,
    tag: "Event"
  }
];

// --- Mock Data: Upcoming Reservations ---
const UPCOMING_RESERVATIONS = [
  { id: 101, room: "Salle Informatique 1", time: "14:30 - 16:30", status: "Approved", date: "Today" },
  { id: 102, room: "Atelier Réseau", time: "08:30 - 10:30", status: "Pending", date: "Tomorrow" },
];

// --- Mock Data: Rooms Live Status ---
const ROOM_STATUS = [
  { name: "Salle Info 1", status: "Occupied", color: "text-rose-400" },
  { name: "Salle Info 2", status: "Free", color: "text-emerald-400" },
];

// --- NEW DATA: My Teachers ---
const MY_TEACHERS = [
  { id: 1, name: "M. El Kharraz", subject: "Dev Digital", isOnline: true, avatar: "bg-indigo-500" },
  { id: 2, name: "Mme. Bennani", subject: "Soft Skills", isOnline: false, avatar: "bg-emerald-500" },
  { id: 3, name: "M. Tazi", subject: "Back-end", isOnline: true, avatar: "bg-purple-500" },
  { id: 4, name: "Mme. Alami", subject: "Anglais", isOnline: false, avatar: "bg-rose-500" },
];

const SchoolDashboard = () => {
  return (
    <div className="min-h-screen bg-slate-950 text-slate-200 font-sans selection:bg-indigo-500 selection:text-white">
      
      {/* --- Top Navigation Bar --- */}
      <nav className="fixed top-0 w-full z-50 bg-slate-950/80 backdrop-blur-md border-b border-white/10 h-16">
        <div className="container mx-auto px-4 h-full flex justify-between items-center">
          <div className="flex items-center gap-2">
            <div className="w-8 h-8 bg-gradient-to-tr from-indigo-500 to-cyan-400 rounded-lg flex items-center justify-center font-bold text-white text-lg">P</div>
            <span className="text-lg font-bold text-white tracking-tight hidden sm:block">PostResa</span>
          </div>

          <div className="flex-1 max-w-md mx-4 hidden md:block">
            <div className="relative group">
              <Search className="absolute left-3 top-2.5 w-4 h-4 text-slate-500 group-focus-within:text-indigo-400 transition-colors" />
              <input 
                type="text" 
                placeholder="Search posts, teachers, or rooms..." 
                className="w-full bg-slate-900 border border-white/10 rounded-full py-2 pl-10 pr-4 text-sm focus:outline-none focus:border-indigo-500/50 focus:ring-1 focus:ring-indigo-500/50 transition-all"
              />
            </div>
          </div>

          <div className="flex items-center gap-4">
            <button className="p-2 text-slate-400 hover:text-white hover:bg-white/5 rounded-full transition-colors relative">
              <Bell className="w-5 h-5" />
              <span className="absolute top-2 right-2 w-2 h-2 bg-rose-500 rounded-full border-2 border-slate-950"></span>
            </button>
            <div className="w-8 h-8 rounded-full bg-gradient-to-br from-indigo-400 to-purple-500 cursor-pointer border border-white/20"></div>
          </div>
        </div>
      </nav>

      {/* --- Main Layout --- */}
      <div className="container mx-auto px-4 pt-24 pb-10 flex gap-8 justify-center">
        
        {/* --- Left Sidebar (Navigation) --- */}
        <aside className="w-64 hidden lg:block fixed left-[max(0px,calc(50%-640px))] top-24 h-[calc(100vh-6rem)]">
          <div className="space-y-1">
            <SidebarItem icon={<Home />} label="Campus Feed" active />
            <SidebarItem icon={<Calendar />} label="My Reservations" />
            <SidebarItem icon={<MapPin />} label="Spaces & Rooms" />
            <SidebarItem icon={<MessageSquare />} label="Messages" />
            <SidebarItem icon={<User />} label="Profile" />
          </div>

          <div className="mt-8 border-t border-white/10 pt-6">
            <h3 className="px-4 text-xs font-bold text-slate-500 uppercase tracking-wider mb-4">Quick Actions</h3>
            <button className="w-full mx-auto flex items-center gap-2 justify-center py-3 bg-indigo-600 hover:bg-indigo-500 text-white rounded-xl font-medium transition-all shadow-lg shadow-indigo-900/20 group">
              <Plus className="w-4 h-4" />
              <span>New Reservation</span>
            </button>
          </div>
        </aside>

        {/* --- Center Column (The Feed) --- */}
        <main className="w-full max-w-xl lg:ml-64 lg:mr-80">
          
          <div className="bg-slate-900/50 border border-white/10 rounded-2xl p-4 mb-6 backdrop-blur-sm">
            <div className="flex gap-4">
              <div className="w-10 h-10 rounded-full bg-gradient-to-br from-indigo-400 to-purple-500 flex-shrink-0"></div>
              <input 
                type="text" 
                placeholder="What's happening on campus?" 
                className="bg-transparent w-full text-slate-300 placeholder:text-slate-500 focus:outline-none"
              />
            </div>
            <div className="flex justify-between items-center mt-4 pt-3 border-t border-white/5">
              <div className="flex gap-2 text-indigo-400">
                <button className="p-2 hover:bg-white/5 rounded-full transition-colors"><div className="text-xs font-medium">📷 Photo</div></button>
                <button className="p-2 hover:bg-white/5 rounded-full transition-colors"><div className="text-xs font-medium">📅 Event</div></button>
              </div>
              <button className="px-4 py-1.5 bg-slate-800 text-slate-300 text-sm font-medium rounded-full hover:bg-slate-700 transition-colors">Post</button>
            </div>
          </div>

          <div className="space-y-6">
            {POSTS.map((post, index) => (
              <motion.div 
                key={post.id}
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: index * 0.1 }}
                className="bg-slate-900/80 border border-white/10 rounded-2xl overflow-hidden hover:border-indigo-500/20 transition-all"
              >
                <div className="p-4 flex justify-between items-start">
                  <div className="flex gap-3">
                    <div className={`w-10 h-10 rounded-full ${post.avatarColor} flex items-center justify-center font-bold text-white text-sm`}>
                      {post.author.charAt(0)}
                    </div>
                    <div>
                      <h3 className="font-semibold text-white text-sm">{post.author}</h3>
                      <p className="text-xs text-slate-400">{post.role} • {post.time}</p>
                    </div>
                  </div>
                  <button className="text-slate-500 hover:text-white"><MoreHorizontal className="w-5 h-5" /></button>
                </div>

                <div className="px-4 pb-3">
                  <div className="mb-2">
                    <span className="inline-block px-2 py-0.5 rounded text-[10px] font-bold bg-indigo-500/10 text-indigo-300 border border-indigo-500/20 mb-2">
                      {post.tag}
                    </span>
                    <p className="text-slate-300 text-sm leading-relaxed">{post.content}</p>
                  </div>
                </div>

                {post.image && (
                  <div className="w-full h-64 bg-slate-800 relative">
                    <img src={post.image} alt="Post content" className="w-full h-full object-cover" />
                  </div>
                )}

                <div className="p-4 flex items-center gap-6 border-t border-white/5 text-slate-400">
                  <button className="flex items-center gap-2 text-sm hover:text-rose-400 transition-colors group">
                    <Heart className="w-5 h-5 group-hover:fill-rose-400/20" /> {post.likes}
                  </button>
                  <button className="flex items-center gap-2 text-sm hover:text-indigo-400 transition-colors">
                    <MessageCircle className="w-5 h-5" /> {post.comments}
                  </button>
                  <button className="flex items-center gap-2 text-sm hover:text-emerald-400 transition-colors ml-auto">
                    <Share2 className="w-5 h-5" /> Share
                  </button>
                </div>
              </motion.div>
            ))}
          </div>
        </main>

        {/* --- Right Sidebar (Widgets) --- */}
        <aside className="w-80 hidden xl:block fixed right-[max(0px,calc(50%-640px))] top-24 h-[calc(100vh-6rem)] space-y-6 overflow-y-auto pb-10 scrollbar-hide">
          
          {/* Widget 1: My Instructors (NEW) */}
          <div className="bg-slate-900/50 border border-white/10 rounded-2xl p-5 backdrop-blur-sm">
            <div className="flex justify-between items-center mb-4">
              <h3 className="text-sm font-bold text-slate-300">My Instructors</h3>
              <button className="text-xs text-indigo-400 hover:text-indigo-300">See all</button>
            </div>
            <div className="space-y-4">
              {MY_TEACHERS.map((teacher) => (
                <div key={teacher.id} className="flex items-center justify-between group">
                  <div className="flex items-center gap-3">
                    <div className={`relative w-9 h-9 rounded-full ${teacher.avatar} flex items-center justify-center text-xs font-bold text-white`}>
                      {teacher.name.charAt(0)}
                      {teacher.isOnline && (
                        <span className="absolute bottom-0 right-0 w-2.5 h-2.5 bg-emerald-500 border-2 border-slate-900 rounded-full" title="Online"></span>
                      )}
                    </div>
                    <div>
                      <p className="text-sm font-medium text-slate-200 group-hover:text-indigo-400 transition-colors cursor-pointer">{teacher.name}</p>
                      <p className="text-[11px] text-slate-500">{teacher.subject}</p>
                    </div>
                  </div>
                  <button className="p-2 rounded-full hover:bg-white/5 text-slate-500 hover:text-white transition-colors" title="Message">
                    <MessageSquare className="w-4 h-4" />
                  </button>
                </div>
              ))}
            </div>
          </div>

          {/* Widget 2: Live Status */}
          <div className="bg-slate-900/50 border border-white/10 rounded-2xl p-5 backdrop-blur-sm">
            <h3 className="text-sm font-bold text-slate-300 mb-4 flex items-center gap-2">
              <span className="relative flex h-2 w-2">
                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                <span className="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
              </span>
              Spaces Live Status
            </h3>
            <div className="space-y-3">
              {ROOM_STATUS.map((room, i) => (
                <div key={i} className="flex justify-between items-center text-sm">
                  <span className="text-slate-400">{room.name}</span>
                  <span className={`font-medium ${room.color} text-xs px-2 py-1 rounded bg-white/5 border border-white/5`}>
                    {room.status}
                  </span>
                </div>
              ))}
            </div>
          </div>

          {/* Widget 3: Your Reservations */}
          <div className="bg-slate-900/50 border border-white/10 rounded-2xl p-5 backdrop-blur-sm">
            <div className="flex justify-between items-center mb-4">
              <h3 className="text-sm font-bold text-slate-300">My Reservations</h3>
              <a href="#" className="text-xs text-indigo-400 hover:text-indigo-300">View all</a>
            </div>
            
            <div className="space-y-3">
              {UPCOMING_RESERVATIONS.map((res) => (
                <div key={res.id} className="p-3 bg-slate-800/50 rounded-xl border border-white/5 hover:border-white/10 transition-colors">
                  <div className="flex justify-between items-start mb-1">
                    <span className="font-bold text-sm text-white">{res.room}</span>
                    {res.status === 'Approved' ? (
                      <CheckCircle className="w-4 h-4 text-emerald-400" />
                    ) : (
                      <Clock className="w-4 h-4 text-amber-400" />
                    )}
                  </div>
                  <div className="text-xs text-slate-400 flex items-center gap-2 mb-2">
                    <Calendar className="w-3 h-3" /> {res.date} • {res.time}
                  </div>
                  <div className={`text-[10px] font-bold px-2 py-0.5 rounded w-fit ${
                    res.status === 'Approved' ? 'bg-emerald-500/10 text-emerald-400' : 'bg-amber-500/10 text-amber-400'
                  }`}>
                    {res.status.toUpperCase()}
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div className="text-xs text-slate-600 px-2">
            <p>© 2025 PostResa • Privacy • Terms</p>
          </div>

        </aside>
      </div>
    </div>
  );
};

const SidebarItem = ({ icon, label, active = false }) => (
  <button className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all ${
    active 
      ? 'bg-indigo-500/10 text-indigo-400 border border-indigo-500/20 font-medium' 
      : 'text-slate-400 hover:text-white hover:bg-white/5'
  }`}>
    {React.cloneElement(icon, { size: 20 })}
    <span className="text-sm">{label}</span>
  </button>
);

export default SchoolDashboard;